# SABSA Foundation Overview Scripts

This folder contains the SABSA overview renderer.

- `Generate_SABSA_Overview_View.ajs` creates a `SABSA - Overview` view from the shared registry.

The renderer shows only entries stored in the model property `SabsaOverviewRegistryJson`. Generator scripts add entries after successful generation, so ungenerated SABSA parts are not shown.
