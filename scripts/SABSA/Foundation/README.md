# SABSA Foundation Scripts

This folder contains foundation-level SABSA scripts that are reusable across SABSA modules.

- `overlays/` contains the current general security overlay generators and tools.
- `overview/` contains the registry-driven SABSA overview renderer.
- `archive/` contains older foundation overlay variants and experimental predecessors.

Use the active scripts in `overlays/` for normal work.
